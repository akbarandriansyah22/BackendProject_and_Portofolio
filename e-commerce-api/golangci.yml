linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - unused
    - ineffassign
    - gosimple
    - gosec
    - errorlint
  issues:
    exclude-rules:
      - path: _test\.go
        linters:
          - gosec
          - errcheck
severity:
  default-severity: error
